<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>
        <variable
            name="data"
            type="com.mobile.travelaja.module.my_booking.eticket.model.Passenger" />
        <variable
            name="position"
            type="Integer" />
    </data>

<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="@dimen/margin_default"
    android:layout_marginEnd="@dimen/margin_default"
    android:layout_marginTop="10dp"
    android:layout_marginBottom="10dp"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/tvName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/TextLato_Normal"
        android:textSize="14sp"
        android:textColor="#24253d"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:text="@{(position + 1) +`.` +data.name()}"
        tools:text="1. Vody Andrian"/>

    <TextView
        android:id="@+id/tvIdCard"
        style="@style/TextLato_Normal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="15dp"
        android:layout_marginTop="6dp"
        android:drawablePadding="5dp"
        android:textColor="#939396"
        android:textSize="10sp"
        android:text="@{`KTP - `+data.idNumber}"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tvName"
        tools:text="KTP - 36858786410001" />
    <TextView
        android:id="@+id/tvType"
        style="@style/TextLato_Normal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="9dp"
        tools:text="Executive 2 / Seat 8B"
        android:textSize="10sp"
        android:text="@{data.seatText}"
        android:textColor="#24253d"
        app:layout_constraintStart_toStartOf="@+id/tvIdCard"
        app:layout_constraintTop_toBottomOf="@+id/tvIdCard" />
    <TextView
        android:id="@+id/tvTypePassenger"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="parent"
        android:background="@drawable/rounded_green_stroke_2"
        style="@style/TextLato_Normal"
        android:textSize="10sp"
        app:layout_constraintTop_toTopOf="parent"
        android:textColor="#24253d"
        android:paddingStart="7dp"
        android:paddingEnd="7dp"
        android:paddingTop="2dp"
        android:paddingBottom="2dp"
        android:text="@{data.type}"
        tools:text="Adult"/>
</androidx.constraintlayout.widget.ConstraintLayout>
</layout>
